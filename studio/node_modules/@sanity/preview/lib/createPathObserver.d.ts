import { FieldName, Path } from './types';
declare type ObserveFieldsFn = (id: string, fields: FieldName[]) => any;
export default function createPathObserver(observeFields: ObserveFieldsFn): (value: any, paths: Path[]) => any;
export {};
